# Quix Project Descriptor
# This file describes the data pipeline and configuration of resources of a Quix Project.

metadata:
  version: 1.0

# This section describes the Deployments of the data pipeline
deployments:
  - name: data-source
    application: 2d-vector-data-source
    version: latest
    deploymentType: Service
    resources:
      cpu: 200
      memory: 500
      replicas: 1
    variables:
      - name: output
        inputType: OutputTopic
        description: Name of the output topic to write into
        required: true
        value: 2d-vector
      - name: ms_to_sleep
        inputType: FreeText
        value: 500
  - name: FMU Integration
    application: fmu-integration
    version: latest
    deploymentType: Service
    resources:
      cpu: 200
      memory: 500
      replicas: 1
    variables:
      - name: input
        inputType: InputTopic
        description: Name of the input topic to listen to.
        value: 2d-vector
      - name: output
        inputType: OutputTopic
        description: Name of the output topic to write to.
        value: fmu-output
  - name: FMU Explorer
    application: fmu-explorer
    version: latest
    deploymentType: Service
    resources:
      cpu: 200
      memory: 500
      replicas: 1
    publicAccess:
      enabled: true
      urlPrefix: fmu-explorer
    network:
      ports:
        - port: 80
          targetPort: 80
    variables:
      - name: port
        inputType: FreeText
        description: Port to run the web server on
        value: 80
  - name: Benchmark CPU-1000
    application: benchmark
    version: latest
    deploymentType: Job
    resources:
      cpu: 1000
      memory: 500
      replicas: 1
    variables:
      - name: output
        inputType: OutputTopic
        description: Topic to publish benchmark results to
        required: true
        value: benchmark-results
      - name: iterations
        inputType: FreeText
        description: Number of timed iterations per benchmark scenario
        value: 100
      - name: warmup
        inputType: FreeText
        description: Warmup iterations before timing begins
        value: 5
      - name: run_label
        inputType: FreeText
        description: Label to tag results (e.g. cpu-200, cpu-500, cpu-1000)
        value: cpu-1000
  - name: Benchmark CPU-2000
    application: benchmark
    version: latest
    deploymentType: Job
    resources:
      cpu: 2000
      memory: 500
      replicas: 1
    variables:
      - name: output
        inputType: OutputTopic
        description: Topic to publish benchmark results to
        required: true
        value: benchmark-results
      - name: iterations
        inputType: FreeText
        description: Number of timed iterations per benchmark scenario
        value: 100
      - name: warmup
        inputType: FreeText
        description: Warmup iterations before timing begins
        value: 5
      - name: run_label
        inputType: FreeText
        description: Label to tag results (e.g. cpu-200, cpu-500, cpu-1000)
        value: cpu-2000
  - name: Benchmark CPU-4000
    application: benchmark
    version: latest
    deploymentType: Job
    resources:
      cpu: 4000
      memory: 500
      replicas: 1
    variables:
      - name: output
        inputType: OutputTopic
        description: Topic to publish benchmark results to
        value: benchmark-results
      - name: iterations
        inputType: FreeText
        value: 100
      - name: warmup
        inputType: FreeText
        value: 5
      - name: run_label
        inputType: FreeText
        value: cpu-4000
  - name: MATLAB Benchmark CPU-1000
    application: benchmark-matlab
    version: latest
    deploymentType: Job
    resources:
      cpu: 1000
      memory: 2000
      replicas: 1
    variables:
      - name: output
        inputType: OutputTopic
        description: Topic to publish benchmark results to
        required: true
        value: benchmark-results
      - name: iterations
        inputType: FreeText
        value: 100
      - name: warmup
        inputType: FreeText
        value: 5
      - name: run_label
        inputType: FreeText
        value: matlab-cpu-1000
  - name: MATLAB Benchmark CPU-2000
    application: benchmark-matlab
    version: latest
    deploymentType: Job
    resources:
      cpu: 2000
      memory: 2000
      replicas: 1
    variables:
      - name: output
        inputType: OutputTopic
        description: Topic to publish benchmark results to
        required: true
        value: benchmark-results
      - name: iterations
        inputType: FreeText
        description: Number of timed iterations per benchmark scenario
        value: 100
      - name: warmup
        inputType: FreeText
        description: Warmup iterations before timing begins
        value: 5
      - name: run_label
        inputType: FreeText
        description: Label to tag results (e.g. matlab-cpu-1000)
        value: matlab-cpu-2000
  - name: MATLAB Benchmark CPU-4000
    application: benchmark-matlab
    version: latest
    deploymentType: Job
    resources:
      cpu: 4000
      memory: 2000
      replicas: 1
    variables:
      - name: output
        inputType: OutputTopic
        description: Topic to publish benchmark results to
        required: true
        value: benchmark-results
      - name: iterations
        inputType: FreeText
        value: 100
      - name: warmup
        inputType: FreeText
        value: 5
      - name: run_label
        inputType: FreeText
        value: matlab-cpu-4000
  - name: Benchmark Compare
    application: benchmark-compare
    version: latest
    deploymentType: Service
    resources:
      cpu: 200
      memory: 500
      replicas: 1
    variables:
      - name: input
        inputType: InputTopic
        description: Topic with benchmark results to compare
        value: benchmark-results

# This section describes the Topics of the data pipeline
topics:
  - name: 2d-vector
  - name: fmu-output
  - name: benchmark-results
